# JS-Fundamentals

A collection of tiny Node.js scripts that demonstrate the very basics of JavaScript:

* constants / `let`
* `process.argv`
* loops
* functions
* recursion

Each file follows the exact requirements of the project (no `var`, only `console.log`, …).

## How to run


node <filename>.js [args…]

